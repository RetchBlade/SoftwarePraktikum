<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp">

    <!-- Profile Picture -->
    <ImageView
        android:id="@+id/profilePicture"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_margin="8dp"
        android:scaleType="centerCrop"
        android:src="@drawable/pp_placeholder"
        android:background="@drawable/circle_shape_full"
        android:clipToOutline="true" />

    <!-- Profile Name -->
    <TextView
        android:id="@+id/profileName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="24dp"
        android:text="Mitbewohner 1"
        android:textColor="@color/own_text_Farbe"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/lifetimePoints"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Punkte: 0"
        android:textColor="@color/own_light_gray"
        android:textSize="14sp" />

    <ImageView
        android:id="@+id/rankIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/einsteiger_ic"/>


    <!-- Remove Button -->
    <ImageView
        android:id="@+id/removeRoommateButton"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center_vertical"
        android:layout_marginRight="8dp"
        android:src="@drawable/ic_remove"
        android:visibility="gone"
        android:contentDescription="Remove Roommate"
        />
</LinearLayout>
