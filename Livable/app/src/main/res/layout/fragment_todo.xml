<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Container für "Heute" -->
    <TextView
        android:id="@+id/todo_heute"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/own_grayish_yellow"
        android:padding="8dp"
        android:text="Heute"
        android:textSize="20sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_today"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"/>

    <!-- Container für "Morgen" -->
    <TextView
        android:id="@+id/todo_morgen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/own_grayish_yellow"
        android:padding="8dp"
        android:text="Morgen"
        android:textSize="20sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_tomorrow"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"/>

    <!-- Container für "Diese Woche" -->
    <TextView
        android:id="@+id/todo_woche"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/own_grayish_yellow"
        android:padding="8dp"
        android:text="Diese Woche"
        android:textSize="20sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_this_week"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"/>

    <!-- Container für "Später" -->
    <TextView
        android:id="@+id/todo_spater"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/own_grayish_yellow"
        android:padding="8dp"
        android:text="Später"
        android:textSize="20sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_later"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"/>

    <!-- Runder Button für Task hinzufügen -->
    <ImageButton
        android:id="@+id/btn_add_task"
        android:layout_width="59dp"
        android:layout_height="63dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:background="@color/black"
        android:src="@android:drawable/ic_input_add" />

</LinearLayout>
